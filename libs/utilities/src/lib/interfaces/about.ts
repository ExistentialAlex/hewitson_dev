export interface WorkHistory {
  title: string;
  dates: number[];
  company: string;
  description: string;
  url: string;
}

export interface Skill {
  id: string;
  skill: string;
  description: string;
}
